# issues.md

## F4 scope fidelity (2026-09-04 12:26)

Audit scope: `E:\progaram\URWPGSim2D` (non-git repo), all files with LastWriteTime >= 2026-09-04 00:00. Read-only audit.

### 1. Allowed-source edit inventory — CONFINED to `Strategy\StrategyQuickRescue\`
Only files there (with LastWrite):
- `StrategyQuickRescue.csproj` 01:13:03, `Properties\AssemblyInfo.cs` 01:13:28
- `obj\*` build caches 01:14:37–38, `obj\Debug\*.dll/.pdb` 09:43:54
- Source: `PathFinder.cs` 01:33:57, `Steering.cs` 03:12:31, `MapConstants.cs` 09:42:35, `PathExecutor.cs` 09:42:46, `Strategy.cs` 09:42:58

No other directory under `Strategy\` has any file >= 09-04 (Strategy\Backup\Old configs untouched since 2025-09-26).

### 2. Untouched-proof — out-of-scope sources
`Common`, `Match`, `StrategyLoader`, `StrategyNew2V2`, `StrategyBallMoving`, `StrategySynchronisedSwimming`, `Strategy\Backup`, simulator core: **0 files** >= 09-04. No new `.exe` anywhere in repo; no `.cs` outside `StrategyQuickRescue`/`.omo`.

### 3. `URWPGSim2D\bin` inventory — expected additions only (24 files)
- `bin\StrategyQuickRescue.dll` + `.pdb` 09-04 09:43:54 — sanctioned deliverables.
- `bin\StrategyCache\StrategyQuickRescue*.dll` x22 (01:57, 02:42, 03:13, 03:25, 03:42, 04:00, 04:35, 04:58, 05:05 + base copy 01:57) — automatic loader cache copies created during sanctioned task-6 live runs; same mechanism as pre-existing cache entries for other strategies (NEUer 20260807*, etc.).
- `bin\config.xml` untouched (LastWrite 2025-09-26).

### 4. Repo-root / stray sweep — one benign artifact
- **Root `config.xml`: CREATED 09-04 02:06:51** (creation == lastwrite), 1524 B, sha1 `68E368A9…`, **byte-identical** to the simulator's own default-config writes from 2025-09-26 (`Strategy\Backup\Old\Strategy1VS1\config.xml` and `Strategy5VS5\config.xml`, same sha1). Attribution: auto-generated by a `URWPGSim2DServer.exe` launch whose CWD was the repo root during task-6 run preparation (02:06 precedes run-0 startup-dialog screenshot 02:17). Content: generic default field config (3000×2000), no code, no secrets. Not a deliberate edit.
- Name sweep `probe|tdump|traceCycle|quickrescue-build|task6-probe` repo-wide: single match = `.omo\evidence\task-6-probe-022248-screen.png` (sanctioned evidence). No leftover probe/tdump executables or sources (per task-1/task-5 evidence, probe .cs/.exe/.config were deleted and verified absent).

### 5. `.omo` meta-area additions (sanctioned): 234 files
- `boulder.json` 1, `start-work\ledger.jsonl` 1, `run-continuation` 58, `notepads\quickrescue-strategy` 4 (problems/issues/learnings/decisions), `plans\quickrescue-strategy.md` 1 (updated 09:52:06), `evidence` 169 (task-1..6 md/txt + screenshots + polllogs + trace CSVs).

### 6. Tooling/harness auto-state (informational, not agent edits)
- `.codegraph\` db files (root .codegraph 3, `Strategy\.codegraph` 3): codegraph indexer state; directories pre-existed (root created 09-03 23:09 during planning session; Strategy since 07-27); re-touched 08:04–08:05 and 09:43 as files changed. Automatic.
- `.omo\run-continuation\ses_*.json` (root .omo + `Strategy\.omo\run-continuation\ses_f97d1d73…` 08:04:53): opencode harness background-task state snapshots (`{sessionID, updatedAt, sources.background-task: idle}`); same pattern as pre-existing files from 09-03 23:06–23:18 and earlier efforts. Automatic.

### Numbered findings
- **F1** `bin\StrategyCache\StrategyQuickRescue 20260904 *.dll` x22 — loader runtime cache from sanctioned task-6 runs. Not a violation.
- **F2** Root `config.xml` (created 02:06:51) — simulator runtime auto-write (launch with CWD = repo root), byte-identical to the sim's own 2025 default writes. Outside strict write areas but **auto-generated, not deliberate**. No violation; recommended optional cleanup by the owning effort (reviewer is read-only).
- **F3** `.codegraph` db updates — indexer auto-state in pre-existing tool directories. Not a violation.
- **F4** `run-continuation` JSONs / `boulder.json` / `start-work` ledger — harness auto-state; the latter two live under `.omo` (sanctioned meta-area). Not a violation.

### VERDICT: **APPROVE**
All deliberate work is confined to `Strategy\StrategyQuickRescue\` plus sanctioned `.omo` notepads/plans/evidence. `bin` additions are the expected `StrategyQuickRescue.dll/.pdb` plus automatic loader-cache copies. Zero source edits in `Common`, `Match`, `StrategyLoader`, `StrategyNew2V2`, simulator core, or any out-of-scope `.csproj`. No stray probe/tdump/debug executables. The only out-of-area artifact (root `config.xml`) is a benign simulator auto-write attributable to the sanctioned live-run evidence; recorded as F2 for optional cleanup, not a scope violation.

